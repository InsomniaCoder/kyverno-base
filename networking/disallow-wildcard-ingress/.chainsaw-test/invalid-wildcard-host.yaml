apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: invalid-wildcard-host
spec:
  rules:
    - host: "*.example.com"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app
                port:
                  number: 80
